# Write your eval set here — a script ending in a call to eval_set().
#
#   from inspect_ai import eval_set
#
#   eval_set(
#       tasks=[...],
#       model=[...],
#   )
#
# Comments rather than an example, because an example would be a guess at what
# you are measuring and would have to be deleted before it could be useful.
#
# Leave log_dir out. Steward decides where a run's logs go — logs/ here, or a
# directory of this workspace's own under the root your machine configured —
# and writes status.md, journal.jsonl and this file out beside the results, so
# that whoever finds the logs finds what explains them. Naming a log_dir is
# still yours to do, and then it is the one Steward uses.
